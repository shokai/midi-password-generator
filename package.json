{
  "name": "midi-password-generator",
  "version": "0.0.1",
  "description": "generate password with MIDI device",
  "main": "index.js",
  "scripts": {
    "test": "eslint src/*.es6 src/*/*.es6",
    "build": "babel src/ --out-dir dist/ --source-maps inline",
    "watch": "parallelshell 'npm run build -- --watch' 'esw src/ --watch'"
  },
  "keywords": [
    "MIDI",
    "password"
  ],
  "author": "Sho Hashimoto <hashimoto@shokai.org>",
  "license": "MIT",
  "dependencies": {
    "midi": "^0.9.3"
  },
  "devDependencies": {
    "babel": "^5.5.8",
    "babel-eslint": "^3.1.17",
    "eslint": "^0.23.0",
    "eslint-watch": "^1.2.4",
    "parallelshell": "^1.1.1"
  }
}
